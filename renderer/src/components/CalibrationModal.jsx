import React from 'react';
export default function CalibrationModal({session}){
  return (
    <div style={{background:'#fff', padding:12, borderRadius:8, marginTop:12}}>
      <h4>Calibration</h4>
      <p style={{color:'#666'}}>Use this to convert raw pot values to mm. (UI placeholder)</p>
    </div>
  );
}
